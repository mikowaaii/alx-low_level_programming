#include "main.h"

/**
 * mul - multiplies two integers
 * @a: int to be multiplied
 * @b: int to be multiplied
 * Return: returns answer
 */
int mul(int a, int b)
{
	int i;
	i = (a * b);
	return (i);
}
